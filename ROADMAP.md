# نقشه راه توسعه — پروژه Bina (بینا)

> خبرخوان تلگرامی ماژولار، چندکاربره، با ترجمه‌ی خودکار و معماری قابل‌توسعه

---

## ۰. اصول حاکم بر معماری

- **هسته + کامپوننت:** یک هسته‌ی مرکزی (Core Engine) که فقط مسئول گردآوری، ذخیره‌سازی و مدیریت چرخه‌ی خبره؛ هر قابلیت کاربری (mute، save، تنظیمات، تصاویر و...) به‌صورت یک کامپوننت مستقل و قابل‌جداشدن پیاده می‌شه، نه کد قاطی‌شده داخل هسته.
- **جداسازی لایه‌ها:** لایه‌ی داده (Storage) ↔ لایه‌ی منطق (Core) ↔ لایه‌ی رابط (Bot/Telegram) کاملاً از هم مستقل باشن؛ رابط تلگرام فقط مصرف‌کننده‌ی Core API‌ه، نه بخشی از منطق.
- **تزریق وابستگی (Dependency Injection):** هر سرویس (مترجم، دیتابیس، زمان‌بند) پشت یک Interface/Protocol تعریف بشه تا جایگزینی (مثلاً تعویض Google Translate با DeepL) بدون دست‌زدن به بقیه‌ی کد ممکن باشه.
- **چندزبانگی از پایه:** هیچ رشته‌ی متنی مستقیم داخل کد نوشته نشه؛ همه از فایل‌های locale (`fa.json` / `en.json`) خونده بشن.

---

## فاز ۰ — راه‌اندازی زیرساخت (پیش‌نیاز همه‌چیز)

| گام | خروجی |
|---|---|
| ساختار ریپو + `pyproject.toml` | اسکلت پروژه با ابزار مدیریت بسته (`uv` یا `poetry`) |
| تنظیم `ruff` + `black` + `mypy` | یکدست‌سازی سبک کد و type-checking |
| `pre-commit` hooks | جلوگیری از commit کد نامرتب/خراب |
| GitHub Actions اولیه (`ci.yml`) | اجرای خودکار lint + test روی هر push/PR |
| فایل `.env.example` | مستندسازی متغیرهای محیطی موردنیاز (بدون secret واقعی) |

---

## فاز ۱ — هسته‌ی مرکزی (Core Engine)

- مدل‌های داده (SQLAlchemy 2.0 async): `Feed`, `Article`, `ArticleTranslation`, `User`, `UserSubscription`, `UserFeedMute`, `SavedItem`, `UserSettings`
- ماژول Fetcher: دریافت و پارس RSS (`feedparser`) با مدیریت خطا و timeout
- ماژول Dedup: تشخیص خبر تکراری بر اساس guid/hash
- ماژول Scheduler: زمان‌بند مستقل از UI (قابل اجرا هم به‌صورت systemd timer، هم داخل خود پردازه با APScheduler)
- سیستم Probation برای فیدهای جدید (طبق تصمیم قبلی: آستانه‌ی خودکار برای ورود به استخر عمومی)

---

## فاز ۲ — لایه‌ی ترجمه

- Interface انتزاعی `TranslatorProvider` با پیاده‌سازی‌های مشخص (Google, DeepL, ...)
- کش ترجمه به‌ازای `(article_id, target_lang)` — طبق تصمیم قبلی، هیچ متنی دوبار برای یک زبان ترجمه نمی‌شه
- تشخیص زبان مبدا (`langdetect` یا `fasttext`)
- Retry + rate-limit handling برای فراخوانی API

---

## فاز ۳ — لایه‌ی بات تلگرام (رابط کاربری)

- فریم‌ورک: `aiogram 3.x` (async، مدرن، پشتیبانی کامل از FSM برای فرم‌های چندمرحله‌ای مثل افزودن فید)
- تحویل خبر از طریق DM خصوصی هر کاربر (نه گروه مشترک — طبق تصمیم امنیتی قبلی)
- سیستم i18n داخلی: هر پیام از `locales/fa.json` یا `locales/en.json` خونده می‌شه؛ زبان رابط کاربری از زبان تلگرام کاربر یا انتخاب دستی تشخیص داده می‌شه
- رعایت استاندارد نگارش فارسی: اعداد فارسی/لاتین طبق تنظیم کاربر، جهت RTL صحیح، فاصله‌گذاری نیم‌فاصله در render متن‌ها

---

## فاز ۴ — کامپوننت‌های کاربری (هرکدوم مستقل و افزودنی)

| کامپوننت | شرح |
|---|---|
| `subscriptions` | مدیریت دسته‌بندی‌های دنبال‌شده |
| `mute` | خاموش‌کردن فید خاص برای یک کاربر |
| `save` | ذخیره/خواندن/حذف خبر |
| `settings` | منوی تنظیمات (زبان ترجمه، دسته‌ها، فیدهای بی‌صدا) |
| `feed_submission` | افزودن فید شخصی توسط کاربر + ورود به چرخه‌ی Probation |
| `media` | نمایش تصویر شاخص خبر (از فید یا og:image) با fallback متنی |

هر کامپوننت باید بتونه مستقل تست بشه و بدون دست‌زدن به بقیه غیرفعال/فعال بشه (feature flag).

---

## فاز ۵ — تست

- **Unit tests:** منطق هسته (dedup، probation، انتخاب زبان) بدون وابستگی به شبکه یا تلگرام
- **Integration tests:** دیتابیس واقعی (SQLite in-memory) + mock کردن API ترجمه و فیدهای RSS
- **Bot tests:** شبیه‌سازی آپدیت‌های تلگرام با `aiogram` test utilities
- هدف پوشش (coverage): حداقل ۸۰٪ روی ماژول‌های Core

---

## فاز ۶ — بسته‌بندی و توزیع

- `pyproject.toml` با entry point CLI (`bina-bot install` / `bina-bot run`)
- اسکریپت نصب که وابستگی‌های سیستم‌عامل (مثل نبود `pip` یا نسخه‌ی پایین پایتون) رو تشخیص و به کاربر گزارش/نصب می‌کنه
- قالب systemd service برای اجرای دائمی روی لینوکس (سازگار با محیط آرچ لینوکس کاربر)
- مکانیزم self-update: چک‌کردن آخرین نسخه از GitHub Releases و اعلام/اعمال به‌روزرسانی طبق تنظیم کاربر

---

## فاز ۷ — مستندسازی

- `README.md` (انگلیسی) + `README.fa.md` (فارسی) — هر دو کامل، همگام، با badge وضعیت CI
- ساختار مستندات به‌گونه‌ای که افزودن `README.<lang>.md` جدید توسط سایر توسعه‌دهنده‌ها ساده باشه
- `CONTRIBUTING.md` با توضیح معماری ماژولار برای مشارکت‌کننده‌های بعدی

---

## ترتیب اجرای پیشنهادی

```
فاز ۰ → فاز ۱ → فاز ۲ → فاز ۳ (نسخه‌ی حداقلی) → تست پایه
   → فاز ۴ (کامپوننت‌ها یکی‌یکی) → فاز ۵ (تکمیل تست‌ها)
   → فاز ۶ → فاز ۷
```

هر فاز باید با یک PR جدا و CI سبز بسته بشه — هیچ فازی روی فاز قبلی که تست نداره سوار نمی‌شه.
